using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class AbilityAssignments : MonoBehaviour
{
    /*[SerializeField] private SelectedSkillUI _selectedSkillUI;
    public SkillSciptableObject _mySkill;
    public SkillSciptableObject _inputSkill;
    public InputAction _inputAction;

    public void SetKey()
    {
        if (_mySkill == null) return;
        
        _inputSkill = _selectedSkillUI._selectedSkill;
        
        Debug.Log(_mySkill.name + " binding is: " + _mySkill._inputAction.GetBindingDisplayString());

        _mySkill._inputAction.ApplyBindingOverride("Z");
        
        Debug.Log(_mySkill.name + " binding switched to: " + _mySkill._inputAction.GetBindingDisplayString());

        Debug.Log(_inputSkill.name + " binding is currently " +_inputSkill._inputAction.GetBindingDisplayString());
        
        Debug.Log(_mySkill.name + " has been switched with " + _inputSkill.name);
        
        _mySkill = _inputSkill;

        _mySkill._inputAction.ApplyBindingOverride(_inputAction.GetBindingDisplayString());
        
        Debug.Log(_mySkill.name + " binding is now: " + _mySkill._inputAction.GetBindingDisplayString());
    }*/
}
